@model List<Ecommerce.Models.Order>

<h2>Your Orders</h2>

@if (!Model.Any())
{
    <p>No orders found.</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Order ID</th>
                <th>Date</th>
                <th>Status</th>
                <th>Items</th>
                <th>Shipping Address</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var order in Model)
            {
                <tr>
                    <td>@order.OrderId</td>
                    <td>@order.CreatedDate.ToString("yyyy-MM-dd")</td>
                    <td>@order.Status</td>
                    <td>
                        <ul>
                            @foreach (var product in order.Products)
                            {
                                <li>@product.Name - ₹@product.Price</li>
                            }
                        </ul>
                    </td>
                    <td>
                        @order.Address?.State, @order.Address?.City, @order.Address?.PostalCode
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
